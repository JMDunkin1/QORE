# All-Year Natural-Gas Strategy Contract

## Scope

`ngas-all-year-beta` is QORE's only public strategy. It is a deterministic daily selector over two internal component ledgers:

1. use the material Summer Alpha row;
2. otherwise use the material Winter Alpha row;
3. otherwise use the shared `US-INDEX-BASKET` fallback target.

A component row is material when it has a non-index thesis, non-zero gas position, or an explicit index-to-cash allocation change. If summer and winter are both material on the same date, artifact generation fails instead of choosing silently. The all-year layer selects targets first and then recomputes the complete UNG/VOO/QQQM execution ledger, so component-local costs cannot decide which lane wins. It adds no threshold search, sizing search, or holdout-driven selection.

## Economic thesis

The winter engine tests the observation that sufficiently broad cold forecasts across the central and eastern United States can raise expected heating demand and natural-gas prices. Longer-lead cold confirmation can support a gas-follow position; as the event approaches or the market overreacts, the engine can use weather resolution and same-direction price behavior for a reversion/fade decision.

The summer engine applies the same demand logic to cooling: fresh, multi-model heat forecasts can support a gas-long overlay, while same-direction rallies can qualify for a storage- and demand-aware fade. Cold forecasts remain an opposing-strength veto and diagnostic only; they cannot authorize a Summer gas short. Because a Summer fade can only reverse a heat-long setup, the executable Summer contract also excludes `reversion-long`. These are tested rules, not assumed causal facts.

## Timing and inputs

- Forecast issue-date signals execute only on later trading sessions.
- Summer live target inference requires a complete common NOAA GFS/GEFS 00z issue set. Winter signal scoring uses GFS, GEFS mean, GraphCastGFS, and AIGFS, while the reviewed heating-demand context can also use ECMWF IFS, ECMWF AIFS, and GEM Global. Selected source lists are derived from the frozen component contract; stale, incomplete, or substituted forecast state fails closed.
- Summer's reviewed temperature statistic is a target-local-day approximation from the exact ordered offsets 06, 12, 18, and 24 UTC relative to target-date midnight, equally averaged and compared with NASA POWER daily-mean T2M. Offset 24 is the following UTC midnight but remains part of the labeled Eastern/Central-US target day. Row and manifest metadata, run 00, lead 7, offsets, aggregation, and baseline statistic are contract-bound. The contract also binds the ordered sample values, valid times, forecast hours, source URL/index identities, digests of fetched index text and selected-field bytes, the exact retained NASA POWER payload digest for each location, each location vector, and the complete location set. Runtime hashes the retained normal file and re-derives `normalMeanF` for the target month/day. A missing, duplicate, reordered, relabeled legacy hours-0, normal-substituted, or cache-incompatible input fails closed. Raw remote forecast bytes that are not retained remain inside the reviewed builder's acquisition-attestation boundary. The checked historical Summer artifacts use the legacy hours-0 snapshot and remain displayable only as development-contaminated research.
- Summer research coverage requires the exact reviewed 18-location eastern-CONUS demand basket and its 1.06 total weight on every active-source issue date. The IDs and weights are versioned once in `scripts/lib/qore-summer-location-universe.mjs`, mirror `data/qore/weather/locations.csv`, and are embedded in the canonical live component contract; a missing, duplicate, unexpected, or reweighted location fails that issue date and blocks component promotion.
- Forecast location rows must contain finite forecast, normal, and anomaly temperatures; forecast and normal means must remain inside the deliberately broad -150F to 160F physical range, and the stated anomaly must agree with forecast minus normal within 0.01F. A failure at any location invalidates the complete source/issue/target/lead atom. Live target inference uses reject mode and fails closed before scoring. Calendar construction, historical optimization, weather QA, and parity replay use quarantine mode, removing both the atom's complete location set and matching score row while recording diagnostics in generated artifacts. Partial use of a corrupt atom is forbidden.
- Every fresh executable score is recomputed from the matching source/issue/target/lead/window/model location atom. It requires exactly the reviewed 18 location IDs and weights and binds the resulting location count, sampled weight, three-decimal weighted anomaly, cold-coverage weight, and extreme count. Fresh runtime and newly built research rows require strict agreement. A one-thousandth anomaly allowance is isolated to retained versioned Winter research calendars for legacy rounding; larger drift, substitution, omission, duplication, or reweighting still invalidates the complete atom.
- QORE's executable spatial representation is the versioned 18-location load-weighted basket and its low-dimensional anomaly, breadth, HDD, and CDD summaries. A freeze-off proxy or other physical load summary may be evaluated only as a separately frozen research shadow; raster maps, CNNs, and other high-capacity spatial models are not part of the reviewed signal contract.
- Live price inputs come from an ignored rolling Yahoo cache of completed sessions. The target-date daily bar is excluded; both gas series must cover the authoritative VOO/QQQM session set, and an unverified intervening weekday fails closed.
- Production inference has one reviewed input profile. Data root, 1991-2020 normal dates, index-basket configuration, file naming, unlimited scan, and the portable GRIB parser are pinned in child processes. Alternate gas/index market files, storage-history and actual-weather files, forecast/market endpoints, nondefault lookbacks, and refresh skips are available only when both `NODE_ENV=test` and the explicit live-inference test capability are set. The fixed, coherence-checked EIA snapshot path remains the legitimate production weather-service handoff. Any reported production test override blocks routing.
- Open-Meteo current forecasts provide operational weather/risk context; they do not set the broker target.
- EIA Lower 48 storage is used only after its versioned WNGSR publication timestamp.
- NASA POWER actual temperatures and 1991–2020 normals support historical anomaly and forecast-quality work.
- Unallocated deployable capital follows the configured VOO/QQQM target-weight index basket. The frozen research execution contract keeps the broker's default 2% cash buffer uninvested.

The exact frozen thresholds, sources, sizing rules, costs, and split dates are authoritative in the component `run-summary.json` files and the executable live contract consumed by `scripts/lib/qore-live-all-year-inference.mjs`. That contract seals the exact Summer target sets and both the Winter target-generator parameters and a digest of its materialized target sets. It also seals the active target-date rules: Summer may emit on May-September sessions or April 25-30 when a lead-7 May forecast becomes tradable after the required one-day entry lag; April 24 is collection-only. Winter may emit only from November 2 through March 31 because a November 1 issue is not tradable until a later session. A common boundary verifies that every nonzero UNG target is one of those emitted lattice points under the active component date policy, rather than merely below a cap. Gas exposure must equal its canonical four-decimal value, index exposure must exactly equal `1 - abs(gasPosition)`, and cash must be exactly zero in both source inference and signal handoff. The signal handoff also binds the canonical source-inference target digest; readiness and the broker reread that configured source snapshot, compare its full target/provenance fields, and include it in the final per-order race check. The all-year artifact stores the canonical contract and digest; paper/live inference fails closed unless that reviewed digest matches the executable contract. The same seal includes the canonical Alpaca execution profile from `config/qore-live-broker-settings.json` and the versioned index basket: deployment/cash buffer, deadband, order mechanics, short policy, data feed, quote/clock/risk limits, and request timeout. Production paper/live routing resolves those values through the same parser and blocks before broker access if an environment override changes the reviewed digest. Change those together; do not describe one contract and execute another.

## Research contract

The summer and winter components retain their own train, validation, and holdout boundaries. Each component selects and determines its eligibility on its own declared train/validation period only. The all-year artifact records each source row's component split separately. Its public `split` is one calendar-wide partition: train ends at the earliest component train end, public holdout starts at the latest component holdout, and the intervening sessions are validation. A separate selection cutoff ends before the earliest component holdout. All-year return-based promotion gates and the selection bootstrap use only that chronological prefix; later composite returns are reporting-only at the all-year layer, while the component-eligibility gates retain each component's declared pre-holdout result. This ensures no component validation row is mislabeled as public holdout and no component holdout return enters the current all-year return-based gate calculation. It does not make those historical periods pristine after repeated strategy development.

All-year eligibility uses a centered circular block bootstrap of every chronological daily active return in that contiguous calendar-wide train/validation prefix versus the index fallback. The UI's Monte Carlo panel is explicitly a report-only full-calendar block-bootstrap diagnostic, not a forecast of live wealth or an independent proof of significance.

### Independent fixed-model reforecast lane

The NOAA GEFSv12 retrospective archive provides a separate, research-only check on the physical demand thesis over the UNG era before QORE's 2021 forecast calendars. `scripts/build-gefs-v12-reforecast-calendar.mjs` reads only the bounded 2 m temperature messages needed for forecast hours 174, 180, 186, and 192; samples the same reviewed 18-location basket; compares the four-sample mean with the retained NASA POWER daily normal; hashes the NOAA index, every selected byte range, every normal payload, and every output record; and writes only beneath `.local/qore/research/` by default. Raw GRIB objects are not retained. Completed date shards can be authenticated and combined with `scripts/merge-gefs-v12-reforecast-shards.mjs`.

`scripts/evaluate-gefs-v12-reforecast.mjs` tests a frozen 12-cell family of simple hot-Summer/cold-Winter demand-follow rules in a 35% UNG-versus-cash sleeve with a 3.2 bp one-way cost. It selects on sessions through 2014, requires positive aggregate and individual-year 2015-2016 validation, at least 20 active validation sessions, positive validation Sharpe, and a family-adjusted block-bootstrap p-value no greater than 0.20, and only then permits the locked winner to be evaluated on 2017-2019. The holdout command cannot evaluate challenger rows. The source is a later-vintage fixed-model reforecast rather than forecasts historically available to an operator, and pre-2020 history cannot reproduce the current QQQM index basket, so this lane validates only a narrow weather/demand thesis. It cannot change the selected all-year artifact, satisfy production parity, or enable paper/live routing.

The 2026-07-22 control-member run completed all 3,865 planned records with no failures. Every candidate lost money through 2014. The train-selected row returned -3.28% in training and +3.01% in 2015-2016, but 2016 was negative and the full-family adjusted p-value was 0.6837. Validation failed, so the 2017-2019 holdout remains unevaluated. The narrow versioned result and binding digests live in `data/qore/research/gefs-v12-reforecast-audit.json`; the reconstructable sampled dataset stays ignored under `.local/qore/research/`.

Invoke the standalone runners directly so this research-only lane does not change the sealed `package.json` production-input digest:

```bash
node scripts/build-gefs-v12-reforecast-calendar.mjs
node scripts/evaluate-gefs-v12-reforecast.mjs
node scripts/evaluate-gefs-v12-reforecast.mjs --reveal-holdout
node scripts/test-qore-gefs-v12-reforecast.mjs
```

### Spatial-demand revision shadow

`spatial-demand-revision-breadth-price-gate-v1` is a separate, one-candidate prospective research registry. It compares the same weather target from the prior calendar day's lead-8 GFS/GEFS 00Z forecasts with the current issue's lead-7 forecasts, using the corrected four-sample target-local-day statistic, exact 18-location demand basket, equal model weights, CDD/HDD revision, directional breadth, and an issue-session `NG=F` already-priced veto. A passing research target is fixed at `+0.25` or `-0.25` gas with `0.75` index and zero cash; Summer negatives remain flat. It is never broker eligible and is not part of the production candidate registry.

The separate local manifest binds the feature, rule, one-candidate registry, reference strategy/artifact core, collector/settlement/evaluator implementations, research execution contract, selected reference ledgers, and every retained normal payload. The append-only writer records before 09:30 New York time, forbids target-day/provisional market bars and backfills, and reproduces valid features and decisions from retained raw atoms before accepting a record. Before each new target, it settles the preceding reviewed target with payload-bound adjusted `UNG`/`VOO`/`QQQM` OHLC; the evaluator replays the complete contiguous prefix against a persistent matched index fallback under all three frozen cost scenarios. The seal still lacks an external chronology anchor, so results remain non-pristine local research. See `docs/spatial-demand-revision-shadow.md` for the exact contract and commands.

## Validation integrity seal

`config/qore-validation-integrity.json` is the reviewed, versioned anti-overfit boundary. It records that all evidence from 2021 through the currently observed 2026 data is development-contaminated: strategy development began on 2026-06-11, data was observed through 2026-07-14, and untouched prospective collection starts on 2026-07-23. Historical train, validation, and holdout labels remain useful for reproducible diagnostics, but none is claimed as pristine out-of-sample evidence.

The immutable preregistration projection binds its own `sealedAt`, the prospective start, the end-to-end strategy-contract digest, canonical all-year artifact-core digest, reviewed broker profile, complete forward-outcome policy and digest, minimum forward/paper gates, a frozen candidate registry, and the reviewed forward-validation implementation. Manifest schema 6 inventories 33 source files and six immutable input files, binds the reviewed NYSE calendar identity, and separately commits the append-only historical prefix of the EIA release calendar. The current registry has exactly one production candidate and a no-post-seal-additions selection policy. Its candidate binds the strategy-contract and fixed-point artifact-core digests; adding a candidate, changing the family size or selection rule, changing an implementation/input byte, or changing any contract rotates the preregistration digest and restarts the prospective window. Mutable evidence, approvals, and `reviewedAt` remain outside that projection so later review does not rewrite the historical candidate seal, but an approval predating the current implementation seal cannot authorize paper or live routing.

The strategy-contract digest covers the component signal/source/sizing contract, canonical exact 18-location universe and score/location aggregate contract for fresh Summer and Winter inputs, all-year selector, production-source target-parity policy, canonical forward-outcome-policy digest, exact research-execution-contract digest, reviewed broker execution/risk profile, and overnight policy. The separately sealed canonical all-year artifact core contains stable contracts, selected results, parity evidence, static gates, and byte/header/row/date bindings for both `selected-trades.csv` and `display-curve.csv`, while excluding generated timestamps, derived status, and mutable prospective evidence/approval fields. Runtime verifies those two ledgers and their common chronological date grid; the Backtest adapter also rejects a display curve whose SHA-256 or shape does not match the reviewed summary. This fixed-point core prevents a digest cycle while making a material artifact mutation invalidate routing.

Forward-evidence schema v4 requires one pre-open commitment and one after-close settlement for every reviewed session from the prospective start through the observation end. The artifact, trusted-writer journal, and every commitment bind the immutable preregistration digest and exact `sealedAt`; inference generation and recording cannot precede that seal, and the seal's New York calendar date must precede the prospective start. Each commitment also binds the exact target fields plus canonical inference, live-inference, signal-handoff, point-in-time input-provenance, strategy-contract, strategy-artifact, execution-contract, and outcome-policy digests. Every nonzero target must be an exact size reachable by the reviewed executable contract under that component's active target-date policy, not merely below a cap. The 10% floor controls episode counting only: a future contract-valid subfloor target would remain in recomputed outcomes but could not count as an episode. The current executable contract emits no such subfloor target, so forged 5% rows fail exact-size validation. Exact zero alone must use the index fallback. Nonzero cash or an index fraction other than one minus absolute gas exposure fails structurally. Material episodes derive from current or preceding-close exposure, with a ten-reviewed-session embargo covering the seven-day forecast lead plus three-day hold. At least two distinct material seasons per component and component-specific leave-one-season-out positivity are required. Heating-season grouping may extend through the April reporting shoulder, but the sealed live target-date policy permits no new material Winter target in April.

The verifier recomputes baseline, elevated, and stress post-cost returns through the sealed shared execution engine against a persistent matched 98%-deployed index fallback. It validates the protected normalized source identifier, row digests, chronology, and calculations, but it cannot independently prove the upstream market bars or input-provenance digests are authentic. Nor can a self-reported local timestamp prove that the preregistration existed historically; that requires an external signed remote commit/tag, transparency log, or WORM/notary timestamp. No production trusted pre-open writer exists yet. Consequently the `trustedPreopenCommitments` gate is always false outside the explicit non-default-path test capability, and production evidence cannot become pristine or unlock live routing until a reviewed writer uses its own New York wall clock with tight skew, atomic one-per-date creation, exact preregistration/live-handoff/input bindings, a recomputable per-observation EIA calendar prefix or hash-chain head, and an externally reviewable seal anchor. The current EIA prefix contract prevents mutation of its 887 sealed rows and permits only plausible weekly extensions, but it is not a WORM guarantee for future suffix rows; those rows cannot support pristine evidence until the writer schema binds them and the contract is resealed. Self-dated or manually backfilled rows cannot satisfy that gate. Live eligibility otherwise requires at least 60 material episodes, at least 15 and two material seasons from each component, two complete prospective Summers and Winters, more than 2% baseline and 1% elevated-cost compounded matched-fallback edge, a stress-cost edge no worse than -2%, the preregistered bootstrap/noninferiority/drawdown/concentration/component-season gates, and explicit live approval bound to the exact sealed strategy and broker profile.

The separately digested Summer fade-ramp challenger is research-only shadow logic: no fade below a 1.50% realized same-direction move, linearly increasing size from zero at 1.50% to the existing reviewed fade size at 2.00%, and unchanged full size above 2.00%. Its frozen comparator embeds and digests the complete active Summer component contract, including signal parameters, implementation rules, position caps, exact location universe, and temporal statistic. It is not part of active candidate selection, the executable component contract, a broker handoff, or either promotion gate, and collection is blocked while the comparator still uses legacy hours-0 inputs. For a compatible future comparator, targets are append-only and accepted only on the active Summer schedule (including its April/May boundary), before 09:30 New York time on a reviewed NYSE session. The writer uses its own clock, permits at most 60 seconds of record-time skew, and exposes a supplied clock only behind the explicit test capability. The frozen calendar contains NYSE's published 2026-2028 full-day closures; Winter-only sessions, weekends, closures, backfills, and dates outside that reviewed horizon fail closed. Each record validates both target projections, allocations, identifiers, strategy/artifact digests, and input-provenance digests before its first permanent write. Targets cannot be relabeled as pristine evidence for a later changed executable contract.

Paper eligibility has its own explicit approval and exact contract-seal gate so paper evidence can be collected before the live evidence gate passes. Paper and live approvals must occur after the current implementation seal; resealing code or inputs makes earlier approvals unusable. Real-capital eligibility additionally requires reviewed paper execution evidence bound to the loaded sealed broker profile and a server-only pseudonymized account: at least 60 complete reviewed sessions, 10 fills, an 80% fill ratio, and four UNG fills with two long and two short. Aggregate and UNG-only median absolute slippage must each be at most 25 bps and both p95 values at most 50 bps, so clean index fills cannot dilute poor gas-leg execution. Every filled order must be at least the reviewed $10 minimum notional, use a reference spread no wider than the reviewed 75 bps maximum, and fill during the regular New York session. Orders and fills must occur after the absolute paper-approval timestamp. When fractional short sales are disabled, a sell that opens or increases a short must use a whole-share quantity and leave a whole-share short position; fractional long purchases and covers remain allowed.

The paper session list must be the complete reviewed NYSE set between its endpoints. Each order binds a canonical broker-record digest; each fill carries coherent side, quantity, and chained pre/post signed positions plus a fresh timestamped bid/ask midpoint. Directional UNG counts derive from those broker fields rather than a declared target direction. Forward evidence uses schema v4 while the paper-evidence wire contract remains schema v2; both live under `.local/qore/validation-evidence/` as owner-only regular files. Runtime recomputes each raw-file SHA-256 and all summaries, and approvals must match the exact strategy and broker-profile digests. These checks do not independently authenticate broker order type, time-in-force, extended-hours setting, shortability, the initial position snapshot, client-order provenance, or the raw broker payload. Those remain explicit manual live-approval responsibilities; raw payloads and account identifiers are never sent to the browser. Test-writer trust is accepted only for an explicitly overridden non-default manifest path, and the test capability itself forces confirmed loopback trading and data endpoints. The checked-in manifest starts all counts at zero and both approvals absent, so paper and live routing remain disabled.

Run the pipeline with:

```bash
npm run train
npm run backtest
```

The authoritative output is `data/qore/research/strategy-agent-runs/ngas-all-year-beta/run-summary.json`; `selected-trades.csv` is the full audit ledger and `display-curve.csv` is its narrow UI projection.

## Research versus execution

Summer Alpha retains Yahoo's continuous `NG=F` proxy only for price confirmation, realized-move gates, and signal-volatility sizing. Every gas return used for Summer selection, Winter selection, and the all-year portfolio is Yahoo `UNG` ETF history. The all-year ledger therefore has one gas P&L contract: `UNG`, matching the Alpaca adapter.

The frozen `config/qore-research-execution.json` contract applies a causal daily timeline. Prior-close holdings earn the close-to-open move; current targets become effective at the split- and distribution-adjusted session open and earn only open-to-close returns. Any same-date close-in weather update used at that open comes from a frozen 00Z run assumed available before the New York session; the historical calendar does not store a separate publication timestamp. Summer and Winter storage logic uses the versioned EIA WNGSR release calendar, including published holiday changes and documented extraordinary delays, and admits a report only at a session open after its publication timestamp. Winter season drawdown uses September as its canonical accumulation boundary. A missing storage release-calendar row fails closed. NASA POWER actual meteorology carries a conservative three-calendar-day availability lag. Turnover is computed separately for UNG, VOO, and QQQM after drift and the broker's default 0.25% rebalance deadband. That deadband may suppress immaterial same-direction drift, but the shared policy always executes UNG entry, exit, direction-change, and target risk-reduction orders; it also carries paired asset reductions with any executable gross increase and forces reductions when the portfolio starts above its deployment envelope. The Alpaca reconciler applies the same rule again at its fresh-quote boundary and blocks if excess exposure cannot be reduced by a valid order. Exactly one frozen baseline cost scenario may rank candidates; elevated and stress costs are reporting-only and cannot be made selection-eligible without changing the versioned contract.

The separate `config/qore-overnight-risk-policy.json` contract tests whether QORE should reduce UNG at the close and restore the ordinary target at the next open. Its 50-policy audit covers fixed retention, long-only and short-only flattening, weekend flattening, and lagged rolling-gap guards under baseline, elevated, and stress friction. Calendar years 2021–2023 train the overlay, 2024 validates it, and 2025 onward is a report-only retrospective period. The pre-2025 winner is the sole research recommendation; later comparisons cannot promote, reject, or replace it. The deployed policy remains `carry-100` until the recommended close-side execution path is separately reviewed and implemented. Reproduce the audit with `npm run research:overnight-risk`.

`carry-100` means no closing-auction or overnight-session order is added: the prior-close UNG holding owns the overnight move and the next causal target still starts at the regular-session open. Alternative auction or extended-hours routes remain research-only unless their own timestamped quote/fill evidence, broker entitlement, order cutoffs, and whole-share constraints are validated.

This is still an idealized once-per-session execution simulation. The live supervisor may reassess an updated target or drift intraday, while the daily Yahoo history cannot reconstruct those quote crossings or fills. Missing historical quotes and fills cannot be recreated, and future gaps, slippage, partial fills, market impact, and borrow availability remain live outcomes. NASA POWER actuals are not archived as immutable historical vintages, so later upstream revisions also cannot be removed retrospectively. ETF expenses and historical futures-roll tracking are already embedded in UNG adjusted prices; taxes remain account-specific and are excluded from the pretax research return.

Negative `UNG` targets require a margin/short-capable Alpaca account, explicit QORE permission, current Alpaca shortability, and whole-share sizing. If those conditions fail, QORE blocks the order rather than changing the strategy.

True `NG`, `MNG`, or `QG` execution remains out of scope until the project has reviewed contract-month selection, expiry and roll logic, margin and price-limit handling, and delivery-risk controls.

## Promotion standard

Treat the checked-in artifact as a research baseline. Paper routing requires paper eligibility; live routing additionally requires pristine prospective evidence and explicit live approval. Raw production-source replay still reproduces all 585 legacy Summer target rows and all 1,362 Winter target rows across component strategy, window, thesis, canonical gas position, and canonical index fraction. That is no longer sufficient for parity: the checked Summer calendars use an hours-0 instantaneous snapshot, so their input-contract gate fails even though target replay matches. Runtime recomputes and content-binds both component replays and their source manifests rather than trusting a generated boolean. Before enabling capital, version complete corrected Summer calendars, rebuild the component and all-year artifacts, obtain exact target-and-input parity, satisfy the return/component/prospective/paper-execution gates, review broker/risk configuration, deploy a clean commit, and complete a successful no-order readiness audit. Live account performance must be evaluated from Alpaca telemetry, never spliced with the research curve.
